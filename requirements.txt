# PyTorch Ecosystem
torch>=2.1.0
torchvision

# Face Analysis & Embeddings
insightface
onnxruntime-gpu

# 3D Mesh & Geometry (trimesh is lightweight; open3d removed)
trimesh
scipy
scikit-learn

# FLAME Kinematics & Skinning (Pure NumPy Rodrigues & LBS; chumpy unpickler handled via _FlameUnpickler)

# Image Processing & Utilities
opencv-python
Pillow
pyyaml
tqdm
matplotlib
kagglehub
loguru
yacs

# Development & Test Suite
pytest
ninja

# NumPy compatibility: code works on NumPy 1.24+ and NumPy 2.x via shims.
# If running legacy binary packages on Kaggle, see DOCS/kaggle_environment.md
numpy>=1.24.0

# ─────────────────────────────────────────────────────────────────────────────
# Non-PyPI GPU Dependencies (Must be installed separately on GPU / Kaggle):
#
# 1. nvdiffrast (NVIDIA Differentiable Rasterizer):
#    pip install git+https://github.com/NVlabs/nvdiffrast.git --no-build-isolation
#    (requires ninja and matching CUDA compiler toolchain)
#
# 2. PyTorch3D (optional, needed only if full SMIRK differentiable renderer is used):
#    pip install "git+https://github.com/facebookresearch/pytorch3d.git"
# ─────────────────────────────────────────────────────────────────────────────
